If we have both the inputs and the outputs, we want to find the **relation** between the inputs and the outputs so we can predicts the output for any new input. It is called Supervised Learning.
Types:
1. [[Regression]]
2. [[Classification]]

Data can of two types:
1. Numerical Data
2. Categorical Data 

if the output is of numerical datatype then it is regression, if categorical then classification.